# Archived growth epilogue — 5 September 2026

The user requested removal of the entire two-part black statue/laptop epilogue:

- “Stop wasting growth hours.” with its light streaks.
- “BOOK A GROWTH CALL ↗” / “and we’ll send the plan.”

## Backup location

`C:\Users\sarth\Desktop\buildanta-site-handoff\removed-growth-sections-2026-09-05`

This is a sibling of the active `buildanta-site` directory, outside the site and its build output. The backup contains pre-removal snapshots of `index.html`, `src/modules/intro.js`, `src/styles/main.css`, and `package.json`, all four desktop/mobile image assets used by these two scenes, and the user's two identifying screenshots. Original asset bytes are preserved.

## Scope

Remove these scenes from the live document and their two-viewport-height scroll hold. Keep the preceding camera, transparent circle, green/human hand sequence, “WE SCALE” design and cozy landscape, and the following note/burn/portal sequence. Other previously retired film scenes are not being restored or redesigned.

## Restore later

Use the archived source snapshots as the source of truth for the removed markup, the `#intro.film-live` CSS block and the epilogue writer/scroll hold in `intro.js`. Restore the matching `consult-shot-think*.webp` and `consult-shot-cta*.webp` assets to `public/assets` if needed. Port only those portions into the then-current files; do not blindly overwrite full files, since that would discard later work. Reinstate the original source fade/world-cut timing together with the film hold. Verify desktop, phone, reverse scrolling and reduced motion after restoring.

The archived source snapshots are supporting context, not a complete independently runnable copy of the site.
